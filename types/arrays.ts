(() => {
  const numbers: number[] = [1, 2, 3, 4, 5];

  numbers.push(6);

  const villians = [
    "Acertijo",
    "Duende verdero",
    "El Diablo",
    "La Muerte",
    "La Tormenta",
  ];

  villians.forEach((v) => console.log(v.toUpperCase()));
})();
